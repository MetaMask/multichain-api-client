/* import type { Transport } from '../types';

export function getStreamTransport(options?: { stream?: any }): Transport {
  return {
    connect: () => {},
    disconnect: () => {},
    request: (data: any) => {},
    onNotification: (callback: (data: any) => void) => {},
  };
} */
